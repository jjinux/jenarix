# here's an example vector addition function

def vector.add(v1,v2):
  [Vector, map(lambda x,y: x+y,v1(1),v2(1))]

entity(Vector, {add:vector.add})

del vector.add

v1 = [Vector, [1,2,3]]

v2 = [Vector, [2,3,4]]

v1(add,v2)

